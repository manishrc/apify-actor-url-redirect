{
  "name": "apify-actor-url-redirect",
  "version": "3.1.2",
  "description": "Actor that takes a list of URLs and provides loaded URLs",
  "dependencies": {
    "apify": "^3.0.2",
    "crawlee": "^3.0.4",
    "normalize-url": "^4.5.1",
    "playwright": "^1.26.1"
  },
  "devDependencies": {},
  "scripts": {
    "start": "node main.js",
    "test": "echo \"Error: oops, the actor has no tests yet, sad!\" && exit 1"
  },
  "author": "@manishrc",
  "license": "MIT",
  "type": "module"
}
