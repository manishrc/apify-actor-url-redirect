{
  "title": "URL Redirects",
  "description": "Actor that takes a list of URLs and provides loaded URLs.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "sources": {
      "title": "Start URLs",
      "type": "array",
      "description": "URLs to resolve",
      "prefill": [
        { "url": "http://manishrc.com" },
        { "url": "http://www.manishrc.com/" },
        { "url": "http://www.manishrc.com/does-not-exist" }
      ],
      "editor": "requestListSources"
    }
  },
  "required": ["sources"]
}
